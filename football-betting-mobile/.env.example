# 复制本文件为 .env 后按需修改，然后重新执行 npx expo start
#
# 只写「网站根地址」：协议 + IP/域名 + 端口
# 不要写 /login、/api/... 等路径（登录接口由 App 自动拼成 /api/auth/login）
#
# 真机 / Expo Go：必须用你电脑的局域网 IP，不能用 127.0.0.1
EXPO_PUBLIC_API_BASE_URL=http://192.168.11.2:5001
